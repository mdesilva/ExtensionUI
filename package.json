{
  "name": "extensionui",
  "version": "1.0.0",
  "description": "Framework for building UIs on top of existing DOMs using Chrome Extension content scripts",
  "scripts": {
    "test": "jest",
    "coverage": "jest --coverage && open coverage/lcov-report/index.html",
    "build": "rm -rf lib/ && ./node_modules/.bin/babel src --out-dir lib --extensions '.ts'"
  },
  "author": "Manuja Desilva",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.14.3",
    "@babel/core": "^7.14.3",
    "@babel/plugin-transform-react-jsx": "^7.14.3",
    "@babel/preset-env": "^7.14.2",
    "@babel/preset-typescript": "^7.13.0",
    "jest": "^26.6.3"
  }
}
